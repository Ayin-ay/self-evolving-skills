# runtime state (changes every conversation, not source)
skill-evolver/logs/state.json
skill-evolver/logs/config.json
skill-evolver/logs/execution-log.md

# pre-modification backups (history lives in evolution-log.md)
skill-evolver/logs/backups/

# personal drafts (private)

# secrets — never commit credentials
opencode.json
*.pem
*_apikey*
*.key
.env